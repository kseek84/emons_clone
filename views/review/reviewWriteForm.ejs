<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Emonsmall review form</title>
    <style> td {text-align:center;} </style>
</head>
<body>
    <%- include("../header.ejs") %>
    
    <div align="center">
        <h1>후기 작성</h1>
        <form action="reviewWritePro">
            <table>
                <tr>
                    <td>상품</td>
                    <td>
                        <select name="itemNo" required>
                            <option value="">상품 번호</option>
                            <% for(var i in itemDB) { %>
                                <option value="<%= itemDB[i]["itemNo"] %>"><%= itemDB[i]["itemNo"] %></option>
                            <% } %>
                        </select>
                    </td>
                </tr>
                <tr>
                    <td>별점</td>
                    <td>
                        <select name="score" required>
                            <option value="">상품 별점</option>
                            <% for(var i = 1; i < 6; i++) { %>
                                <option value="<%= i %>"><%= i %></option>
                            <% } %>
                        </select>
                    </td>
                </tr>
                <tr>
                    <td>후기 내용</td>
                    <td style="padding:50px">
                        <textarea rows="10" cols="50" name="content" required></textarea>
                    </td>
                </tr>
                <tr>
                    <td>파일</td>
                    <td style="padding-left:100px">
                        <input type="file" name="image" required>
                    </td>
                </tr>
                <tr>
                    <td colspan="2">
                        <input type="submit" value="작성완료">
                    </td>
                </tr>
            </table>
        </form>
    </div>
    
    <%- include("../footer.ejs") %>
</body>
</html>